#version 460
#extension GL_EXT_mesh_shader : require
#include "voxel.glsl"

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

taskPayloadSharedEXT uvec3 task_position;

void main() {
    task_position = uvec3(2, 2, 2) * gl_GlobalInvocationID;
    EmitMeshTasksEXT(1, 1, 1);
}